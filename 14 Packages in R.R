
# When you want to intall packages in r then we go to packages in right bottom 
# option other wise we can write code like as 

install.packages("ggplot2")
library(ggplot2)   # this line is used to activate the installed package